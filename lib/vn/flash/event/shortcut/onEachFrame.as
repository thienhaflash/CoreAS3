package vn.flash.event.shortcut 
{
	import vn.flash.event.EnterFrame;
	/**
	 * ...
	 * @author thienhaflash
	 */
	public function onEachFrame(f: Function, params: Array = null): void { EnterFrame.onEach(f, params); }
}